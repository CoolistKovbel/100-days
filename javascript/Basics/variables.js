// 1. cant define a variable more than once
let petName = "Murko";
petName = "Chicken";
console.log(petName);

// 2. there are rules related to variable names
let test_ = 2;
let result = 3 + 4;

// 3. variables names cannot be reserved keywords
