// SPDX-License-Identifier: MIT


pragma solidity ^0.6.0;

contract MyContract {

    bool boolValue = true;

    function foo() external {

        // Just like in javascript

        // if(!boolValue && boolValue || boolValue !== true) {
        //     // ...
        // } else{
        //     // ...
        // }

        // forloop like in javascript
        // for(uint i = 0; i < 10; i++) {
        //     i = 1...10
        // }

        // While loop like in javascript
        while(boolValue) == false || {
            // run a computation
            if() {
                boolValue = false;
                break;
            }
            if() {
                continue;
            }
        }



    }

}


